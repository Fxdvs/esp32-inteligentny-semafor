#pragma once

namespace Sensor {        
  void init();                                      // Inicializácia
  void update();                                    // Update 
  float getDistance();                              // Vratenie vzdialenosti
}