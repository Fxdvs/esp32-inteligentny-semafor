#pragma once

namespace Button {        
  void init();                                      // Inicializácia
  void update();                                    // Update 
  bool isPressed();                                 // Zistenie stlacenia 
}